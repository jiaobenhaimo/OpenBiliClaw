# M84 Popup Tabs 实现计划

1. 后端接口
   - 新增 `GET /api/profile-summary`
   - 新增 `POST /api/chat`
   - 为 API 补充对应 schema 与测试

2. Popup 结构改造
   - `popup.html` 增加 tab bar 和三块视图容器
   - 新增 `popup-api.js`，封装 health / recommendations / profile / chat / feedback 请求

3. 推荐 tab 修复
   - 取消整卡点击
   - 改为内容区和“打开视频”按钮可跳转
   - 确保反馈按钮与 comment 输入区不会触发跳转

4. 画像 tab
   - 拉取 `/api/profile-summary`
   - 展示画像摘要、核心特质、深层需求、兴趣关键词
   - 未初始化时显示引导

5. 聊天 tab
   - 构建轻量消息列表 + 输入框 + 发送按钮
   - 对接 `/api/chat`
   - 处理加载和错误状态

6. 文档与验证
   - 更新 extension/changelog/todolist/index
   - 运行：
     - `ruff check src/ tests/`
     - `mypy src/`
     - `pytest -q`
     - `npm test`
     - `npm run typecheck`
     - `npm run build`
